# config/fx_mapping.yaml
# Per-track FX device design reference for building an XRNS template.
# The current Lua runtime does not parse this file. Verify placeholders manually.

tracks:
  "1":  # drums
    fx:
      - name: "Comp"
        device: "#Compressor"
        params: [threshold, ratio, attack, release]
      - name: "Rev"
        device: "#Reverb"
        params: [dry_wet, room_size, decay]
  "2":  # breaks
    fx:
      - name: "Dist"
        device: "#Distortion"
        params: [drive, tone, dry_wet]
      - name: "Delay"
        device: "#Delay"
        params: [time, feedback, dry_wet]
  "3":  # bass
    fx:
      - name: "Filter"
        device: "#Filter"
        params: [cutoff, resonance]
  "4":  # lead
    fx: []
  "5":  # pads
    fx:
      - name: "Rev"
        device: "#Reverb"
        params: [dry_wet, room_size]
  "6":  # stabs
    fx: []
  "7":  # fx
    fx:
      - name: "Rev"
        device: "#Reverb"
        params: [dry_wet]
      - name: "Delay"
        device: "#Delay"
        params: [dry_wet]
      - name: "Bit"
        device: "#Cabinet"
        params: [drive]
  "8":  # vox
    fx: []
  master:
    fx:
      - name: "Filter"
        device: "#Digital Filter"
        params: [cutoff, resonance]
