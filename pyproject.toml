[project]
name = "renoise-ai-dj"
version = "0.1.0"
description = "OSC bridge and Renoise tooling for AIDJ live performance"
requires-python = ">=3.10"
license = { file = "LICENSE" }
dependencies = [
  "python-osc>=1.8,<2",
  "PyYAML>=6,<7",
]

[project.optional-dependencies]
midi = [
  "mido>=1.3,<2",
  "python-rtmidi>=1.5,<2",
]

[tool.setuptools]
packages = []
