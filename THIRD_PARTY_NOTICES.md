# Third-Party Notices

The MIT license in `LICENSE` covers this repository's original code and documentation only.

It does not grant rights to redistribute:

- Renoise or its factory content
- Akai controller firmware, manuals, or artwork
- third-party samples, breaks, vocals, presets, plugins, or XRNS files
- material created by users and loaded into Renoise

The turnkey builder generates simple fallback waveforms from mathematical functions and does
not embed third-party audio. Replace them only with audio you are licensed to use.

Runtime Python dependencies retain their own licenses:

- `python-osc`: The Unlicense
- `PyYAML`: MIT

Official external references are listed in `docs/references.md`.
